# シナリオテスト（手動）

このアプリを**実際にブラウザで声を出して**動作確認するための手順書です。
音声ループは自動テストでは確認できないため、ここは人が声で試します。
（`/ask` の自動テストは `cd backend && uv run pytest` で別途確認できます。）

担当エージェントの違う **2つのシナリオ** を用意しています。

- **TC-1**: `qa_agent`（既存の工場故障グラフに **質問** する）
- **TC-2**: `registration_agent`（新しい故障ケースを **聞き取り→登録候補** にする）

---

## 共通の前提条件（両テスト共通）

すべて満たしてから開始してください。

1. **Neo4j 起動済み**：`brew services start neo4j`（データ投入済み。詳細は `LANGCHAIN_AGENT_CONTEXT.md`）
2. **バックエンド起動済み**：`cd backend && uv run uvicorn app:app --port 8000 --reload`
3. **フロント起動済み**：`cd frontend && npm run dev`
4. **`.env`** に有効な `OPENAI_API_KEY` がある
5. ブラウザで **http://localhost:3000**（自動で **/v1** に移動）を開いている
6. **マイクを許可**できる状態（静かな環境・イヤホン推奨でハウリング防止）

> 用語や仕組みは `GUIDE.md` の §4「音声とテキスト」を参照。
> テキスト（吹き出し）は文字起こしで、声とは別に生成される点に注意。

---

## TC-1: 既存グラフへの質問（qa_agent）

| 項目 | 内容 |
|---|---|
| 対象エージェント | `qa_agent`（ツール: `query_graphdb` / Neo4j read-only） |
| 目的 | 声の質問が qa_agent に振り分けられ、グラフの正しい答えが声とテキストで返ることを確認 |
| 使うデータ | ポンプA / 過熱 → 原因 潤滑不足 → 対策 **再潤滑**（`factory_failures.csv` より） |

### 手順

1. 「会話をはじめる」を押し、マイクを許可する（状態表示が「接続しました。話しかけてください。」になる）。
2. はっきりと話す：**「ポンプAが過熱したときの対策は？」**
3. AI の返答（声）を聞き、画面の吹き出しを確認する。

### 期待結果（チェックリスト）

- [ ] **右の吹き出し（あなた）** に、話した内容がほぼ正しく文字起こしされて出る
      （例:「ポンプAが過熱したときの対策は？」）
- [ ] 数秒後、**AI が声で回答**する（スピーカーから聞こえる）
- [ ] **左の吹き出し（エージェント）** に回答テキストが出る
- [ ] 回答の内容に **「再潤滑」** が含まれる（＝グラフの正解と一致）
- [ ] 実在しない設備の作り話をしていない（例：勝手な機種名・数値を言わない）

### 追加確認（任意）

- [ ] 別の質問でも正しく答える：**「モーターAの過熱の対策は？」→「負荷見直し」** が返る
- [ ] バックエンドのログに `qa_agent` への振り分けと Cypher 実行の形跡がある

### 合否

- **合格**: 上記の必須チェックがすべて ✅
- **不合格の主因と対処**:
  - 回答が「Neo4j query failed」→ Neo4j 未起動/接続情報ミス（`GUIDE.md` §11）
  - 右の吹き出しが出ない → ユーザー文字起こし未有効（`transcribe_model`）/マイク未許可

---

## TC-2: 新しい故障ケースの登録（registration_agent → Neo4j 書き込み）

| 項目 | 内容 |
|---|---|
| 対象エージェント | `registration_agent`（4項目を聞き取り、確認後 `register_case` で **Neo4j に書き込む**。未確定なら `draft_registration_record` で下書き） |
| 目的 | 登録依頼が registration_agent に振り分けられ、4項目を聞き取り、確認後に **実際に Neo4j へ登録**され、ノードが増えることを確認 |
| 使うデータ（新規・未登録） | equipment=**ポンプC** / failure=**過熱** / cause=**冷却不足** / action=**冷却系点検** |

### 事前準備：現在のノード数を控える

Neo4j Browser（http://127.0.0.1:7474）で実行：

```cypher
MATCH (n) RETURN count(n) AS nodes
```

初期データなら **40**（Equipment9 + Failure15 + Cause8 + Action8）。

### 手順（すべて音声。ボタンなし）

1. 接続した状態で話す：**「新しい故障ケースを登録したい」**
2. AI が**不足項目を聞き返す**。声で答える：
   **「ポンプCの過熱です。原因は冷却不足、対策は冷却系点検」**
   （※ 数回に分けて聞かれたら、その都度答える）
3. 4項目がそろうと、画面に**読み取り専用の確認表**（設備／故障／原因／対策）が出て、
   AI が音声で **「下記の情報で登録してよろしいでしょうか」** と聞く。
4. （任意）誤変換があれば**声で訂正**する。例:「原因は潤滑不足です」
   → 表が更新され、もう一度同じ確認をされる。
5. **声で「はい」**（または「登録して」）と答える。
6. AI が **「ナレッジに登録しました。」** と言い、確認表が消える。

### 期待結果（チェックリスト）

- [ ] 最初の依頼で **registration_agent** に振り分けられる（qa_agent ではない）
- [ ] 4項目がそろうまで、AI が**足りない項目を聞き返す**
- [ ] 4項目がそろうと **読み取り専用の確認表**が出て、AI が「下記の情報で登録してよろしいでしょうか」と聞く
- [ ] **「はい」と言うまで Neo4j に書き込まれない**（確認前は書かない）
- [ ] 声で**訂正**すると表が更新され、もう一度確認される
- [ ] 「はい」の後に **「ナレッジに登録しました。」** と言い、確認表が消える
- [ ] AI が承諾前に「登録しました」と**作り話をしない**

### 追加確認（★ 実際に書き込まれたことの検証）

**A. 声で確認（TC-1 の要領）**：**「ポンプCの過熱の対策は？」**
- [ ] **「冷却系点検」** が返る（＝登録した内容を qa_agent が読めている）

**B. Neo4j Browser（http://127.0.0.1:7474）で確認**：以下を貼って実行。

登録したレコード（4つの値）：
```cypher
MATCH (e:Equipment {name:'ポンプC'})-[:HAS_FAILURE]->(f)-[:CAUSED_BY]->(c)-[:RESOLVED_BY]->(a)
RETURN e.name AS equipment, f.name AS failure, c.name AS cause, a.name AS action
```
- [ ] ポンプC / 過熱 / 冷却不足 / 冷却系点検 の1行が返る

追加されたノードと関係（経路そのもの）：
```cypher
MATCH p=(:Equipment {name:'ポンプC'})-[:HAS_FAILURE]->()-[:CAUSED_BY]->()-[:RESOLVED_BY]->()
RETURN [n IN nodes(p) | n.name] AS nodes, [r IN relationships(p) | type(r)] AS rels
```
- [ ] nodes = `[ポンプC, 過熱, 冷却不足, 冷却系点検]`
- [ ] rels = `[HAS_FAILURE, CAUSED_BY, RESOLVED_BY]`

ノード数：
```cypher
MATCH (n) RETURN count(n) AS nodes
```
- [ ] **42**（＋2：Equipment「ポンプC」と Failure「過熱(ポンプC)」が新規。
      Cause「冷却不足」/ Action「冷却系点検」は既存ノードを共有するので増えない）

### 後片付け（任意）

テストデータを消す場合：
```cypher
MATCH (e:Equipment {name:'ポンプC'})-[:HAS_FAILURE]->(f:Failure {equipment:'ポンプC'})
DETACH DELETE e, f
```
または `cd graphdb && uv run load_graph.py` で CSV から作り直す（★グラフ全体を初期化）。

### 合否

- **合格**: registration_agent に振り分け ＋ 聞き返し ＋ 確認表 ＋「はい」で登録 ＋
  「ナレッジに登録しました。」＋ ノードが 40→42 ＋ qa_agent が読める
- **不合格の主因と対処**:
  - いきなり qa_agent 的に検索した → Supervisor の振り分けプロンプトを見直す
  - 「はい」の前に登録された／進行状況を作り話する → registration_agent・音声側の指示を確認
  - 訂正しても表が更新されない → 会話記憶（checkpointer / session_id）が効いているか確認
  - 「Neo4j write failed」 → Neo4j 未起動・接続情報（`app_config.json`）を確認

---

## 結果記録テンプレート

| テスト | 実施日 | 実施者 | 結果 (OK/NG) | メモ（気づき・再現手順） |
|---|---|---|---|---|
| TC-1 qa_agent | | | | |
| TC-2 registration_agent | | | | |

> NG のときは、状態表示に出る `/realtime/calls` のHTTPステータスや、バックエンドのログ、
> ブラウザのコンソール（F12）のエラーもメモしておくと原因追跡が早いです。
