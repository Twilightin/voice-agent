# Copy to .env and fill in. .env is gitignored (never committed).
OPENAI_API_KEY=sk-your-key-here

# Local Neo4j password. Overrides app_config.json's "neo4j.password" placeholder.
# Set this so the app can connect to your local Neo4j.
NEO4J_PASSWORD=your-neo4j-password
